import {default as createEngine, Fact, Rule, Operator, Engine, Almanac, OperatorDecorator } from './json-rules-engine'

export default createEngine;
export { Fact, Rule, Operator, Engine, Almanac, OperatorDecorator }